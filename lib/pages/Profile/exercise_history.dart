import 'package:flutter/material.dart';

class ExerciseHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Histórico de Exercícios"),
    );
  }
}
