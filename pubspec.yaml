name: health_app
description: A Flutter health tracking app.

publish_to: 'none' # Remova se desejar publicar
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.2.1  # Versão estável que resolve o conflito
  provider: ^6.0.5
  image_picker: ^0.8.7+3


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
